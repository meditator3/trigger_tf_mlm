name: Deploy InfraStructure

on:
  repository_dispatch:
    types: ["dispatch_tf"]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}
      - run: |
          chmod o+x ./start.sh
          ./start.sh    
          echo ${{ github.event.client-payload.service }}
          echo ${{ github.event.client-payload.revision }}